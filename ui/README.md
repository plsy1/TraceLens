# TraceLens UI

The UI is a Vite/React frontend wrapped by Tauri 2. It currently renders a
static dashboard so the visual shell can be reviewed before the local API is
connected.

```bash
npm install
npm run dev
```

To run the desktop shell after installing the Tauri system prerequisites:

```bash
npm run tauri dev
```
